<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <%- include('header') %> <!-- Include the header if you have one -->

    <div class="container">
        <h1 class="my-4">User Details: <%= user.username %></h1>
        
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Full Name: <%= user.fullname %></h5>
                <p class="card-text"><strong>Username:</strong> <%= user.username %></p>
                <p class="card-text"><strong>Address:</strong> <%= user.address %></p>
                <p class="card-text"><strong>Sex:</strong> <%= user.sex === 'M' ? 'Male' : 'Female' %></p>
                <p class="card-text"><strong>Email:</strong> <%= user.email %></p>
            </div>
        </div>

        <a href="/users" class="btn btn-primary my-3">Back to User List</a>
    </div>

    <%- include('footer') %> <!-- Include the footer if you have one -->
</body>
</html>
